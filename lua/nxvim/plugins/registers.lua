-- https://github.com/tversteeg/registers.nvim

-- { == Configuration ==> =====================================================

require("registers").setup({
	window = {
		max_width = 100,
		highlight_cursorline = true,
		border = "rounded",
		transparency = 0,
	},
})
-- <== }
